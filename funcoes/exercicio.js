const qtAlunos = parseInt(prompt("Digite a quantidade total de aluos"))
const alunosPresentes = parseInt(prompt("Digite o total de alunos presentes"))

const alunosFaltantes = (param1 - param2) => {
    const faltantes = param2 - param2
    return faltantes
}